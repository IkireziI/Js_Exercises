function average(arr){
    let total= 0;
    for (i = 0; i<arr.length; i++){
        total += arr[i];
    }
    console.log( total);
    avg = total/arr.length;
    console.log(avg)
}

let avg = [1,2,3,4];
average(avg);
   
//average
