function smallest(arr){
    let small = arr[0];
    for (i=0; i<arr.length; i++){
        if (arr[i] < small){
            small = arr[i];
        }
    }
     return small;
}

let array = [1,2,3,4];
console.log( smallest(array));

//smallest array
